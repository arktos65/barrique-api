apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "barrique-api.fullname" . }}-test-connection"
  labels:
    {{- include "barrique-api.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "barrique-api.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
