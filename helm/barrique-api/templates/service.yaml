apiVersion: v1
kind: Service
metadata:
  name: {{ include "barrique-api.fullname" . }}
  labels:
    {{- include "barrique-api.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "barrique-api.selectorLabels" . | nindent 4 }}
